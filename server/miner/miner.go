package miner

import msg "bitcoin_miner/message"

func SubmitJob(request *msg.Message) (chan *msg.Message, error) {
	//TODO

	/*
		select {
					case job := <-urgentJobs:
						urgent(job)
					case job := <-trivialJobs:
						trivial(job)
					}
	*/

	//Dividir o nonce em bloquinhos ☺️


	return nil,nil
}
